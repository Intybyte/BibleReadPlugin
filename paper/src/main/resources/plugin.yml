name: BibleReadPlugin
version: '${version}'
main: me.vaan.bibleread.paper.BibleReadPlugin
api-version: '1.16'

permissions:
  biblereadplugin.getbook:
    default: op
  biblereadplugin.update:
    default: op